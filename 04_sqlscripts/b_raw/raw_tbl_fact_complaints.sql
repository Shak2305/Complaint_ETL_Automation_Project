CREATE OR REPLACE TABLE raw_fact_complaints (
    CASE_ID VARCHAR,
    CLIENT_ID VARCHAR,
    LOGGED_ON DATE,
    CLOSED_ON DATE,
    AGE_DAYS INT,
    OPEN_DAYS INT,
    YEAR INT,
    MONTH INT,
    QUARTER INT,
    WEEK INT,
    CASE_STATUS VARCHAR,
    PRODUCT_LINE VARCHAR,
    INTAKE_SOURCE VARCHAR,
    CHANNEL VARCHAR,
    RISK_FLAG BOOLEAN,
    REG_TYPE VARCHAR,
    DECISION_STATUS VARCHAR,
    OVERALL_OUTCOME VARCHAR,
    REOPEN_COUNT INT,
    ACK_SLA_MET BOOLEAN,
    REVIEW_SLA_MET BOOLEAN,
    FINAL_SLA_MET BOOLEAN,
    VULNERABLE_FLAG BOOLEAN,
    FIRST_CONTACT_RESOLVED BOOLEAN,
    BOUNCE_FLAG BOOLEAN,
    GOODWILL_AMOUNT NUMBER(18,2),
    COMP_AMOUNT NUMBER(18,2),
    ADJUSTMENT_AMOUNT NUMBER(18,2),
    TOTAL_AMOUNT NUMBER(18,2),
    EXTERNAL_TOTAL_COST NUMBER(18,2),
    OWNER_ID VARCHAR,
    LOGGED_BY_ID VARCHAR,
    CLOSED_BY_ID VARCHAR
);
