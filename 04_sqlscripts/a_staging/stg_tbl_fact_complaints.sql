CREATE OR REPLACE TABLE stg_fact_complaints (
    CASE_ID VARCHAR,
    CLIENT_ID VARCHAR,
    LOGGED_ON VARCHAR,
    CLOSED_ON VARCHAR,
    AGE_DAYS VARCHAR,
    OPEN_DAYS VARCHAR,
    YEAR VARCHAR,
    MONTH VARCHAR,
    QUARTER VARCHAR,
    WEEK VARCHAR,
    CASE_STATUS VARCHAR,
    PRODUCT_LINE VARCHAR,
    INTAKE_SOURCE VARCHAR,
    CHANNEL VARCHAR,
    RISK_FLAG VARCHAR,
    REG_TYPE VARCHAR,
    DECISION_STATUS VARCHAR,
    OVERALL_OUTCOME VARCHAR,
    REOPEN_COUNT VARCHAR,
    ACK_SLA_MET VARCHAR,
    REVIEW_SLA_MET VARCHAR,
    FINAL_SLA_MET VARCHAR,
    VULNERABLE_FLAG VARCHAR,
    FIRST_CONTACT_RESOLVED VARCHAR,
    BOUNCE_FLAG VARCHAR,
    GOODWILL_AMOUNT VARCHAR,
    COMP_AMOUNT VARCHAR,
    ADJUSTMENT_AMOUNT VARCHAR,
    TOTAL_AMOUNT VARCHAR,
    EXTERNAL_TOTAL_COST VARCHAR,
    OWNER_ID VARCHAR,
    LOGGED_BY_ID VARCHAR,
    CLOSED_BY_ID VARCHAR
);
